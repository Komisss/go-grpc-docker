syntax = "proto3";

package example;

option go_package = "example/servicepb";

service HelloService {
  rpc SayHello (HelloRequest) returns (HelloResponse);
}

message HelloRequest {
  string name = 1;
  string language = 2; // Новое поле для языка
}

message HelloResponse {
  string message = 1;
}